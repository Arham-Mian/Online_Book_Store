# Cucumber discovery + reporting settings for JUnit Platform
cucumber.glue = com.example.bookstore.bdd.steps
cucumber.features = classpath:features

# Plugin list: pretty console + summary + JUnit-format XML output
cucumber.plugin = pretty,summary,junit:target/cucumber-junit.xml
